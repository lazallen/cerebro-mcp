# Personal Config and Context

This directory is gitignored (except this file). It's the right place for:

- `heartbeat-config.json` — your local heartbeat task schedule
- `smart-meetings-config.json` — your managed meeting definitions
- Any personal workflow notes, prompts, or context files

Do not put personal config in tracked files. Keep the repo clean for others.
